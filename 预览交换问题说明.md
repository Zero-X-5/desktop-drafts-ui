# 拾笺桌面应用：预览左右交换时"目录页被交换"问题

> 请忽略中间轮次的尝试结论，直接看**问题现象 → 期望 → 核心矛盾 → 寻求方案**。

## 一、应用与技术栈

- **框架**：Tauri 2（Rust 后端 + Windows WebView2 渲染前端）
- **前端**：原生 HTML / CSS / JS（无框架、无构建），单 WebView 页面
- **窗口特性**：透明无边框窗口
  - `transparent: true`、`decorations: false`、`shadow: false`
  - `backgroundColor: "#00000000"`（WebView 背景透明）
- **系统**：Windows 11，**200% 显示缩放（DPI=192）**
- **窗口尺寸（逻辑 px）**：收起 248×36；展开 248×480；预览 720×480
  - 200% 缩放下，逻辑 720 → 物理 1440；逻辑 248 → 物理 496

## 二、界面结构（WebView 内）

```
.app-window（容器）
├── .collapsed-bar       收起条
└── .expanded-shell
    ├── .window-toolbar   工具栏
    └── .window-content
        ├── .directory-view   目录列表（宽 248 逻辑）
        └── .editor-panel     编辑区（预览时占其余）
```

## 三、期望行为（用户明确要求）

1. 窗口在屏幕**左半** → 布局 `[目录][编辑]`
2. 窗口被拖到**贴近屏幕右缘**、编辑区触到屏幕边缘时 → **自动交换**为 `[编辑][目录]`
3. **交换发生时：目录页的屏幕坐标必须完全不动**，只有预览页（编辑区）跳到目录的另一侧
4. 窗口在右半时方向反过来（`[编辑][目录]`），反向同理
5. 交换动作**平滑不突兀**（理想像浏览器里 div 移动那样）

## 四、当前实现（JavaScript + Tauri API）

两套 CSS 布局，通过切换 `preview-left` / `preview-right` 类实现：

```css
/* preview-right：目录在左、编辑在右 */
.app-window.preview-right .directory-view { left: 0; }
.app-window.preview-right .editor-panel    { left: 249px; }

/* preview-left：目录在右、编辑在左 */
.app-window.preview-left .directory-view { left: auto; right: 0; }
.app-window.preview-left .editor-panel    { left: 0; right: 248px; }
```

交换逻辑（保持目录屏幕坐标）：

```js
async function applyPreviewSide(side) {
  const monitor = await currentMonitor();
  const scale = monitor.scaleFactor;
  const dirLeft = await getDirLeftPhysical();      // 当前目录左缘（物理）
  const pos = await win.outerPosition();
  previewSide = side;
  appEl.classList.toggle('preview-left', side === 'left');
  appEl.classList.toggle('preview-right', side === 'right');
  const w = 720 * scale;
  const x = side === 'right' ? dirLeft : dirLeft - (w - 248 * scale);
  await win.setPosition(new PhysicalPosition(x, pos.y));   // 系统 API，瞬间
}
```

- `getDirLeftPhysical()`：preview-right 时返回窗口左缘；preview-left 时返回窗口左缘 + 472*scale
- `win.onMoved`：拖动窗口时监听，编辑区触到 `currentMonitor()` 左右缘才触发交换

## 五、遇到的问题

1. **目录视觉上"被交换"**
   交换后目录在**窗口内**从左侧翻到右侧（preview-left 目录必须在右）。虽然**屏幕坐标保持**（窗口左移 944px 补偿），但用户盯着窗口内容，看到目录"从左跑到右"，认为"目录和预览对调了"。

2. **窗口瞬间跳变**
   交换瞬间窗口要整体左移 944 物理 px（预览 1440 物理宽，目录保持），`setPosition` 是系统级瞬间移动，视觉突兀。

3. **核心逻辑矛盾**
   **"目录在窗口内位置不动" 与 "编辑跳到目录左侧" 互斥**：
   - 编辑在目录**左** → 目录必须在窗口**右** → 目录窗口内位置必然翻转
   - 目录永远在窗口**左** → 编辑只能在目录**右** → 编辑永远到不了目录左

4. **透明窗口背景合成**（已解决，仅供参考）
   transparent 窗口上，opaque CSS 背景（如 `#0e121b`）**不会合成到屏幕**，显示为 WebView 默认浅色；必须用半透明 `rgba(14,18,27,0.95)` 才正常显示。

5. **DPI**：窗口尺寸须用 `LogicalSize`，位置须用物理像素（`PhysicalPosition`）配合 `scaleFactor`。

6. **多屏**：边缘判断用 `currentMonitor()`（窗口所在显示器），跨屏边界行为仍可能不理想。

## 六、已尝试的方案与结果

| 方案 | 做法 | 结果 |
|---|---|---|
| A. 取消左右翻转 | 目录永远窗口左、编辑永远右，窗口整体左移避让 | 用户否决（要求编辑能到目录左） |
| B. 平滑动画 | `setPosition` 逐帧 12 步 / ~170ms 渐变 | 用户觉得不好，已回退 |
| C. 当前 | 直接 `setPosition` 瞬间交换，目录屏幕坐标保持 | 目录窗口内翻转 + 窗口跳变，用户不接受 |

## 七、寻求的解决方案

在 **Tauri 2（真实 OS 窗口 + WebView2）** 技术栈下：

1. 如何实现"**目录屏幕坐标完全不动、只有编辑跳到另一侧**"，同时**视觉上目录不翻转/不突兀**？
2. 是否有办法让**目录在窗口内也固定**（不翻转），同时**编辑能到目录左侧**？
   - 例如：窗口加宽为多列布局，编辑区用 CSS transform 在目录"后面"滑过？窗口不移动、编辑单独动画？
   - 或者：Tauri 窗口本身能否实现局部区域移动/动画，而非整窗 setPosition？
3. 原版浏览器 HTML 里这是普通 div 定位（同步、平滑、可随便动），Tauri 里"窗口=OS 窗口"是最大差异点。有没有办法绕过"整窗移动"，让预览交换只在 WebView 内容层完成？

## 八、期望的最终效果（验收标准）

1. 交换瞬间，**目录在屏幕上的位置像素级不动**
2. 编辑区平滑从一侧切到另一侧
3. 全程不出现"目录跟着跑 / 目录翻转"的视觉
4. 多屏（双屏并排）下也符合预期
