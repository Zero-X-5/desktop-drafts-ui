# Shijian Assets

This directory contains runtime application assets.

Expected files:

- `icon.png` - tray/runtime icon
- `icon.ico` - Windows application icon

The source design image is currently stored in:

`design-assets/shijian-icon-source.png`

Before packaging the first installer, convert the source icon into Windows multi-size ICO format and place it here.
