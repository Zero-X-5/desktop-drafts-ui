# AGENTS.md

本文件适用于整个 `desktop-drafts-ui` 项目。所有开发、修改、测试和文档工作都必须遵守以下规则。

## 1. 核心原则

- 优先完成用户明确提出的目标，不擅自扩大需求。
- 选择最简单、直接、可维护的实现，不为假设中的未来需求提前设计复杂架构。
- 代码必须易读、易懂、命名清晰；宁可拆出小函数，也不要堆叠难以理解的逻辑。
- 修复根本原因，不用临时遮盖、重复判断或无意义补丁掩盖问题。
- 保持改动小而集中，不顺手重构无关模块，不修改无关样式和行为。

## 2. 思考与执行效率

- 开始前只读取与当前任务直接相关的文件；先搜索定位，再查看必要上下文。
- 简单任务直接执行；只有多阶段、存在依赖或风险较高的任务才创建计划。
- 不反复读取已经确认且未变化的内容，不进行无目的全仓库扫描。
- 不为了“更完整”增加用户没有要求的功能、依赖、抽象层或配置。
- 工具调用应按目的合并，避免重复命令、重复搜索和重复验证。
- 遇到可合理推断的小问题自行判断；只有影响产品方向、数据安全或架构选择时才询问用户。

## 3. 代码规范

- TypeScript、React、CSS 和 Rust 均遵循项目现有风格及各自官方惯例。
- 使用完整、有意义的英文名称，避免单字母变量、含糊缩写和过长函数。
- 单个函数只处理一个明确职责；重复逻辑出现两次以上时再考虑抽取。
- 优先使用平台和语言原生能力，新增第三方依赖前必须确认确有必要。
- 不添加无意义注释；注释只说明不能从代码本身看出的原因、约束或取舍。
- 不保留失效代码、废弃选择器、调试日志、临时文件或未使用资源。
- 用户数据、路径、文件名和外部输入必须校验；错误应有明确且可恢复的反馈。

## 4. 项目边界

- Web 界面负责展示、交互和状态表达；Windows 系统能力由 Tauri/原生层负责。
- HTML 原型中的置顶、托盘、开机启动和目录选择必须明确标记为模拟，不得宣称已具备原生能力。
- 文件读写、重命名、删除、目录监听和系统托盘通过统一的桌面 API 边界调用，UI 不直接散落系统逻辑。
- TXT 文件是用户真实数据；写入优先采用安全保存策略，删除优先进入系统废纸篓。
- 界面保持轻量，不引入与桌面便签核心用途无关的账号、云端、数据库或服务端能力。

## 5. UI 与交互

- 遵循 `DESIGN_SYSTEM.md`，保持 Apple/macOS 风格的层级、间距、字体、材质和交互一致性。
- 优先使用系统字体、统一图标体系和已有设计变量，不随意新增近似颜色或尺寸。
- 高频操作直接可见，低频操作进入菜单；危险操作与普通操作分组并明确提示。
- 所有按钮必须有可理解的标签或 `aria-label`，键盘操作必须提供清晰焦点状态。
- 同时验证浅色、深色、减少动态效果和降低透明度状态。
- 动画只用于说明状态变化，保持短促，并尊重 `prefers-reduced-motion`。

## 6. 自我验证

- 修改后必须运行与改动最相关的最小验证，再根据风险决定是否扩大范围。
- JavaScript/TypeScript 修改至少执行语法或类型检查；样式和交互修改至少进行一次浏览器实际验证。
- 涉及构建配置、依赖或桌面原生代码时执行对应构建检查。
- 不使用 Computer Use、鼠标模拟、截图识别或其他 Windows 桌面自动化验证本项目。
- 不为验证而自动点击、拖动、输入或关闭拾笺及其他桌面窗口；桌面界面与原生行为由用户人工验收。
- 原生窗口、托盘、置顶、开机启动、多显示器和系统文件交互只做代码检查、单元测试、`cargo check` 或构建验证。
- 需要桌面人工验证时，提供简短、按顺序执行的验收清单，并明确预期结果，不代替用户操作。
- 只有用户在当前任务中明确要求恢复桌面自动化验证时，才允许临时使用，并仅验证用户指定范围。
- 新增行为时优先补充邻近测试；项目已有测试体系时不得仅靠人工判断。
- 验证失败最多针对同一问题迭代三次；仍失败时停止扩大修改并准确说明阻塞原因。
- 不修复与当前任务无关的失败，但必须在交付说明中指出。
- 未实际运行的测试不得声称通过，模拟能力不得声称为真实系统能力。

## 7. 安全与文件操作

- 不执行破坏性删除、覆盖或批量移动，除非用户明确要求且目标路径已确认。
- 不提交密钥、令牌、个人路径、用户草稿内容或本地配置。
- 不主动创建 Git 提交、分支或推送，除非用户明确要求。
- 不修改生成目录、依赖目录或构建产物，除非任务明确涉及这些内容。

## 8. 交付要求

- 最终说明保持简洁，只包含完成内容、关键文件、验证结果和必要限制。
- 文件引用使用准确路径和起始行号。
- 不粘贴大段已写入文件的代码，不重复描述显而易见的实现细节。
- 如有合理下一步，只推荐一个优先级最高的动作，不一次罗列大量延伸需求。
