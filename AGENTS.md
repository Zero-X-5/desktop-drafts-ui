# AGENTS.md

本文件适用于整个 `desktop-drafts-ui` 项目。所有开发、修改、测试和文档工作都必须遵守以下规则。

## 1. 核心原则

- 优先完成用户明确提出的目标，不擅自扩大需求。
- 选择最简单、直接、可维护的实现，不为假设中的未来需求提前设计复杂架构。
- 代码必须易读、命名清晰；优先修复根本原因，不用临时补丁掩盖问题。
- 保持改动小而集中，不顺手重构无关模块，不修改无关样式和行为。

## 2. 技术方向

- Web 界面负责展示、交互和状态表达；桌面系统能力由独立桌面壳或原生层负责。
- 不强制绑定 Electron、Tauri 或其他框架；技术选择以当前阶段目标、视觉复现度和维护成本为准。
- 当前阶段使用 Electron 作为最小桌面效果验证壳，因为它可以直接使用 Chromium 加载现有 HTML。
- 渲染层不得直接启用 Node.js；系统能力必须通过 `preload.js` 暴露的最小 API 边界调用。
- HTML 原型中的保存、回收站、托盘、开机启动和目录选择，在接入真实系统能力前必须明确标记为演示行为。

## 3. 代码规范

- JavaScript、HTML 和 CSS 遵循项目现有风格及各自官方惯例。
- 使用完整、有意义的英文名称，避免含糊缩写和过长函数。
- 单个函数只处理一个明确职责；重复逻辑出现两次以上时再考虑抽取。
- 优先使用平台和语言原生能力，新增第三方依赖前必须确认确有必要。
- 不保留失效代码、废弃选择器、调试日志、临时文件或未使用资源。
- 用户数据、路径、文件名和外部输入必须校验；错误应有明确且可恢复的反馈。

## 4. 项目边界

- 当前阶段只验证真实桌面窗口中的视觉与交互一致性，不提前实现完整数据层。
- 目录页是窗口布局的固定屏幕锚点；预览页只能在目录两侧切换，不得与目录页交换。
- TXT 文件将是后续阶段的真实用户数据；写入应采用安全保存策略，删除优先进入系统废纸篓。
- 文件读写、目录监听、托盘和全局快捷键必须通过统一桌面 API 调用，不能散落在 UI 代码中。
- 不引入与桌面草稿核心用途无关的账号、云端、数据库或服务端能力。

## 5. UI 与交互

- 遵循 `DESIGN_SYSTEM.md`，保持现有 Apple/macOS 风格的层级、间距、字体、材质和交互一致性。
- 优先复用现有 CSS 变量、SVG 图标和尺寸，不随意新增近似颜色或规格。
- 高频操作直接可见，低频操作进入菜单；危险操作与普通操作分组并明确提示。
- 所有按钮必须具有可理解的标签或 `aria-label`，键盘操作必须有清晰焦点状态。
- 同时考虑浅色、深色和 `prefers-reduced-motion`；动画只用于说明状态变化并保持短促。

## 6. 自我验证

- 修改后运行与改动最相关的最小验证，再根据风险决定是否扩大范围。
- JavaScript 修改至少执行 `node --check`；HTML/CSS 修改至少检查资源引用和关键 DOM ID。
- 涉及 Electron 主进程或 preload 时，必须检查 IPC 参数校验、`contextIsolation`、`sandbox` 和 `nodeIntegration` 设置。
- 不使用桌面自动化代替用户验收；透明窗口、拖动、置顶、多显示器和系统材质由用户按 `STATUS.md` 人工检查。
- 未实际运行的测试不得声称通过，演示能力不得宣称为真实文件或系统能力。

## 7. 安全与文件操作

- 不执行破坏性删除、覆盖或批量移动，除非用户明确要求且目标路径已确认。
- 不提交密钥、令牌、个人路径、用户草稿内容或本地配置。
- 不修改依赖目录或生成目录；`node_modules`、构建产物和日志不得提交。
- 不主动合并到默认分支；开发工作优先放在独立分支或草稿 PR 中。

## 8. 文档与交付

- 每次阶段性修改后同步更新 `STATUS.md`，记录完成内容、限制、验证结果和下次接续入口。
- 架构或交互基线发生变化时同步更新 `README.md` 或 `DESIGN_SYSTEM.md`。
- 最终说明只包含完成内容、关键文件、验证结果和必要限制。
