# Demo 视觉规范

- 真实玻璃顶栏高度：84px 逻辑像素（随系统 DPI 缩放）。
- Tab 栏高度：52px 逻辑像素，使用不透明表面。
- 主窗口：1000×650，圆角 18px。
- 顶栏只保留“拾笺”、置顶、设置和关闭按钮。
- 材质切换按钮只放在编辑区的 Demo 控制区域，不属于真实顶栏。
- 编辑区使用不透明表面，避免把系统 backdrop 误认为编辑区材质。
- Mica 与 Acrylic 由系统绘制，不叠加网页截图、渐变背景或 CSS 模糊。
- 文字和图标使用深色主题下的浅色，浅色主题下的深色。

## 视觉边界

- 顶栏是唯一使用 DWM 原生材质的区域；Tab 栏和编辑区必须保持不透明，避免层级混淆。
- 不叠加网页截图、远程背景图、CSS 模糊或自制渐变来模拟 Acrylic/Mica。
- 顶栏文字和按钮应位于同一客户区坐标系，不能因为 DPI、跨屏或窗口缩放出现重复、错位或残影。
- 顶栏底部不增加额外白线、阴影或系统默认缩放外框；任何边界线必须有明确的窗口层级原因。
- `Mica`、`Acrylic`、`透明玻璃` 是验证材质差异的三种状态，不改变 Tab 栏和编辑区的实体表面规则。
