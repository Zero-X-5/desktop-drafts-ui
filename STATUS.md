# STATUS

> 最后更新：2026-07-31  
> 当前分支：`agent/minimal-electron-desktop-shell`

## 当前阶段

**阶段 1：Electron 真实桌面效果验证壳。**

目标是让现有 HTML 原型作为真实无边框桌面窗口运行，先人工判断视觉、透明材质、拖动和预览换边是否符合预期。当前不是完整可用的草稿应用。

## 本次完成

- 将原有项目约束从“必须使用 Tauri”调整为桌面框架中立，并明确当前验证壳采用 Electron。
- 使用 Electron `BrowserWindow` 直接加载现有 HTML、CSS 和 JavaScript，不引入 React、Vue、TypeScript、数据库或打包器。
- 建立 `main.js`、`preload.js` 和最小 `package.json`。
- 配置无边框、透明、不可手动缩放的真实桌面窗口。
- Windows 11 优先调用 Acrylic；调用失败时保留透明 CSS 回退。
- 启用 `contextIsolation` 和 `sandbox`，关闭渲染层 `nodeIntegration`。
- 通过 preload 只暴露窗口上下文、布局、置顶、最小化和移动通知接口。
- 收起状态使用 `248 × 36`；目录状态使用 `248 × 480`；预览状态使用 `720 × 480`。
- 预览换边以目录页为屏幕坐标锚点：目录不交换，只有预览页切换左右侧。
- 修正 Electron 248px 窗口误触发原型 48px 移动端目录宽度的问题。
- 将模拟桌面背景和任务栏隐藏，真实透明区域直接显示桌面内容。
- 保留 `styles.css` 和 `app.js` 作为稳定入口；具体实现拆分到 `styles/` 和 `scripts/`。
- 补充 `DESIGN_SYSTEM.md`，固定窗口尺寸、目录锚点和动效规则。

## 尚未实现

- TXT 文件创建、读取、自动保存、重命名与删除。
- 目录选择、目录监听和外部修改冲突处理。
- 系统托盘、全局快捷键和开机启动。
- 窗口位置持久化与多显示器恢复。
- 安装包、应用图标、签名和自动更新。
- Acrylic / 纯透明材质的应用内切换开关。

## 验证结果

已完成：

- `main.js`、`preload.js`、`app.js` 和 `scripts/` 下全部 JavaScript 的 `node --check` 语法检查。
- `package.json` JSON 解析检查。
- HTML 本地资源引用检查。
- 确认渲染代码不再依赖 Tauri 包或 API。
- 检查主进程 IPC 的窗口尺寸与坐标参数边界。

当前环境未执行：

- `npm install`
- Electron 窗口实际启动
- Windows Acrylic、拖动、置顶和多显示器人工验收

原因：当前执行环境没有安装项目依赖，也不是 Windows 桌面验收环境。不能把静态检查描述成桌面运行验证。

## 已知限制

- Acrylic 只在支持该材质的 Windows 版本上生效；其他环境会使用透明窗口与 CSS 材质。
- CSS 阴影可能受到透明窗口边界裁切，需要在真实 Windows 环境确认。
- Electron 拖动区域使用 `-webkit-app-region: drag`；标题文字保留为 `no-drag`，用于触发现有双击展开/收起行为。
- 当前示例草稿刷新应用后会恢复。保存状态、回收站、文件夹和冲突提示仍是原型演示。
- 当前未生成 `package-lock.json`，首次执行 `npm install` 后应提交锁文件。

## 人工验收清单

1. 在 Windows 项目目录执行 `npm install`，然后执行 `npm start`。
   - 预期：桌面右上角出现 `248 × 36` 的拾笺收起条，没有浏览器地址栏或系统标题栏。
2. 双击“拾笺”文字。
   - 预期：窗口向下扩展为 `248 × 480` 的目录页，目录左上角屏幕位置不跳动。
3. 拖动标题图标或标题栏空白区域。
   - 预期：移动的是真实桌面窗口，而不是页面里的假窗口。
4. 在屏幕右侧点击任意草稿。
   - 预期：预览页在目录左侧打开，目录仍停留在原屏幕位置。
5. 关闭预览，将目录拖到屏幕左侧，再点击草稿。
   - 预期：预览页在目录右侧打开。
6. 保持预览打开并将窗口拖过屏幕中线。
   - 预期：停止移动后预览换到空间更充足的一侧，目录不与预览交换且没有明显跳动。
7. 点击置顶按钮并切换到其他应用。
   - 预期：拾笺保持在最前；再次点击后恢复普通层级。
8. 点击最小化按钮。
   - 预期：窗口进入系统任务栏并可正常恢复。
9. 切换浅色和深色主题。
   - 预期：布局尺寸不变，圆角、菜单、编辑器和透明材质正常。

## 下次接续入口

优先完成 Windows 人工验收并记录以下差异：

- Acrylic 与原 HTML 视觉是否接近。
- 透明区域是否出现黑边、白底或闪烁。
- 圆角和阴影是否被窗口边界裁切。
- 展开、关闭和预览换边时目录是否有可见位移。
- 100%、125%、150% Windows 缩放下的尺寸与坐标是否一致。

确认桌面效果可接受后，再进入 **阶段 2：真实 TXT 数据层**。
