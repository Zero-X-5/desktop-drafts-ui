# 状态

- 更新日期：2026-08-13
- 更新 Agent：Codex
- 对应 HEAD commit：f8cca24
- 当前分支：agent/native-glass-demo

## 当前目标

验证无系统缩放外框的原生 DWM 顶栏效果，确保白色外框消失且窗口仍可缩放；继续定位跨屏和拉伸后的 DWM/GDI 残影。

## 已完成

- 建立独立原生 Demo 目录。
- 采用 Windows DWM `DWMWA_SYSTEMBACKDROP_TYPE` 实现 Mica/Acrylic 切换。
- 加入无边框顶栏、窗口拖动、圆角和材质按钮。
- 已构建并启动 x64 Demo。
- 已连续切换 Mica、Acrylic、透明玻璃、Mica，窗口始终响应。
- 修复高 DPI 下顶栏只显示为薄条的问题：窗口和全部布局指标统一按当前 DPI 缩放。
- 修正 Demo 结构：真实玻璃顶栏恢复为 84 逻辑像素，下面增加 52 逻辑像素的不透明 Tab 栏；材质切换按钮移入编辑区控制区域。
- 修复 `CW_USEDEFAULT` 导致窗口启动在屏幕底部的问题：窗口现在按工作区居中，并由运行脚本关闭旧实例、激活新实例到前台。
- 修复截图验证误差：截图工具优先使用目标窗口 `PrintWindow`，不再把 Edge/Codex 的屏幕区域误认为 Demo 内容。
- 已重新构建并启动验证：右侧控件顺序为置顶、设置、关闭；Tab 栏位于顶栏下方且不使用玻璃材质；Mica、Acrylic、透明玻璃切换均保持响应。
- 修复玻璃只出现在最上方圆角区域的问题：通过 `DwmExtendFrameIntoClientArea` 将 DWM frame 扩展到客户区，顶栏保留透明绘制，Tab 栏和编辑区继续覆盖实体表面。
- 修复材质测试脚本旧坐标问题；已验证 DWM 类型按 Mica=2、Acrylic=3、透明玻璃=1 正确切换。
- 按微软自定义窗口框架流程处理 `WM_NCCALCSIZE`，并通过 `SWP_FRAMECHANGED` 立即移除旧非客户区；顶栏自绘内容已恢复显示。
- 补回自定义框架的边缘缩放命中测试，避免去除默认非客户区后窗口无法调整大小。
- 最新版本已重新构建并启动；截图验证可见顶栏标题、右侧控件、Tab 栏和编辑区。
- 移除窗口样式 `WS_THICKFRAME`，不再让 DWM 系统缩放外框参与绘制。
- 增加客户端自定义缩放：边缘光标、鼠标捕获、四边和四角拖动、最小尺寸限制。
- 实际屏幕捕获确认白色外框消失；直接拖动验证宽度可增加 120 像素，极限收缩稳定限制为 760×480 逻辑像素。
- 默认启动后连续切换 Mica、Acrylic、透明玻璃、Mica，进程始终响应。
- 明确将原“纯色”模式命名为“透明玻璃”：`DWMSBT_NONE` 在当前只扩展顶栏 frame 的结构下就是绝对透明效果，继续保留该模式。
- 修复顶栏移动/缩放后的 DWM/GDI 残影：合并窗口位置变化后的完整重绘请求，移动/缩放结束时调用 `RedrawWindow` 与 `DwmFlush` 同步合成，并让 `WM_ERASEBKGND` 只清理不透明 Tab/编辑区，避免旧像素残留到透明顶栏。
- 重新构建并启动验证：三种材质连续切换正常；自动执行 40 次移动、30 次缩放并恢复原位置和尺寸后，窗口仍响应，尺寸和位置均正确。
- 修复跨显示器后的 DWM 残影：`WM_DPICHANGED` 使用新 DPI 建议矩形并提交 `SWP_FRAMECHANGED`，随后重新应用材质属性、扩展顶栏 Frame、完整刷新并等待 DWM 合成；`WM_DISPLAYCHANGE` 增加显示器拓扑变化兜底。
- 真实跨屏回归通过 3 轮：窗口在 192 DPI 主屏和 96 DPI 副屏之间往返，DPI 每次正确经历 `192 -> 96 -> 192`，窗口持续响应且尺寸保持稳定。
- 当前代码已回退到 `f8cca24` 的局部顶栏 DWM Frame 实现；此前尝试的“全窗口 Frame + 取消擦除”方案已撤销，未作为已验证修复保留。

## 未完成与限制

- 当前是 Win32/DWM 材质验证，不是最终 WinUI 3 + WebView2 壳。
- 用户实际观察到跨屏返回和拉伸后仍会出现重复标题、Tab 横条和不会消失的残影；此前自动验证只覆盖窗口消息、尺寸和响应性，不能证明像素级视觉问题已解决。
- 残影的候选根因包括局部 `DwmExtendFrameIntoClientArea`、`WM_NCCALCSIZE` 全客户区、自绘 GDI 与 DWM 重定向表面重叠，以及连续 `SetWindowPos` 的合成时序；当前尚未完成结构性修复。
- 只支持 Windows 11 Build 22621+ 的系统 backdrop API。
- 尚未接入拾笺 React 页面。
- 截图 API 无法可靠呈现 DWM backdrop，最终材质仍需要在实际窗口中观察；截图可用于确认布局和自绘内容是否为空白。

## 下一步

先建立可重复的像素级验证，再针对 DWM Frame/客户区边界选择结构性修复方案；修复确认后再决定是否创建 WinUI 3 + WebView2 迁移壳。

## 交接信息

- 当前运行方式：在 Windows 11 x64 环境执行 `build.ps1` 构建，必要时由用户手动执行 `run.ps1` 观察窗口。
- 当前未完成：跨第二屏往返、改变 DPI、连续拉伸后的残影仍需在真实窗口中复现和验证。
- 下一位 Agent 不应把历史“跨屏回归通过”当作视觉修复结论；应先用截图或像素差异确认问题是否仍在。
