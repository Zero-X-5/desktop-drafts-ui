# 拾笺：1192 轨道窗口方案 —— 实施反馈

> 前文《预览交换问题说明.md》描述的"目录被交换"问题，已按你建议的 **1192 轨道窗口方案**做了 Step 1 原型。以下是实施结果与实测问题，请据此给下一步建议。

## 一、技术栈（不变）

- Tauri 2 + WebView2，原生 HTML/CSS/JS（无框架）
- 透明无边框窗口：`transparent:true`、`decorations:false`、`shadow:false`、`backgroundColor:#00000000`
- **200% 显示缩放（DPI=192）**，物理尺寸 = 逻辑 × 2
- 系统当前为**浅色主题**（`data-theme="light"`），CSS 变量 `--glass-strong` / `--surface` 均为浅色半透明值

## 二、已实施（Step 1 原型，代码已跑通）

**窗口结构**：预览态窗口固定 **1192 逻辑宽（2384 物理）**，内容：

```
┌────472────┬─248─┬────472────┐
│ 编辑区    │ 目录 │ 编辑区    │
│ (transform) │居中  │ (transform)│
└───────────┴──────┴───────────┘
```

- 目录恒在 `left:472px; width:248px; z-index:2`
- 编辑区恒 `left:0; width:472px; z-index:1`
  - `preview-left`  → `transform: translateX(0)`
  - `preview-right` → `transform: translateX(720px)`
- **交换只切换 CSS 类，不再调用 `setPosition()`**
- 窗口位置：`winX = 目录屏幕坐标 - 472×scale`（保持目录屏幕坐标）
- 预览态窗口背景：`transparent`

## 三、实施结果（实测通过的核心目标）

| 验收点 | 实测结果 |
|---|---|
| 目录屏幕坐标不变 | ✅ 全程恒定（400 → 收起 → 再展开 仍 400） |
| 交换不移动窗口 | ✅ 只切类，窗口位置不动 |
| 编辑平滑滑动 | ✅ CSS `transition: transform 210ms` |
| 目录窗口内不翻转 | ✅ 恒居中 472，永不左右跑 |

## 四、实测遇到的问题（核心反馈）

1. **透明区域仍有可见残留**
   preview 时窗口背景已设为 `transparent`（`getComputedStyle` 确认 `rgba(0,0,0,0)`），但**另一侧透明区域（472 逻辑宽）在桌面上仍肉眼可见"一层东西"**，不是纯透明。疑似：
   - WebView2 透明窗口上，半透明内容区（目录/编辑）的合成层残留
   - 或窗口边缘/`::before` 边框层/圆角在 1192 宽窗口上的表现
   - 需要确认透明区域到底残留的是什么（背景层？内容 ghost？合成伪影？）

2. **窗口内容基本完全透明**
   preview 时窗口背景被设为 `transparent` 后，目录（`var(--glass-strong)` 半透明）和编辑（`var(--surface)` 半透明）**叠在透明窗口上透明度过高**，草稿文字几乎看不清。即：**"透明模式"下内容太透，不可读**。

3. **透明区域鼠标穿透未处理**
   尚未做 `SetWindowRgn`，透明区域（472 逻辑）当前会拦截鼠标（未实测穿透，因第 1、2 点问题已决定先反馈）。

## 五、当前关键 CSS/JS（供参考）

```css
.app-window.expanded.preview-mode,
html[data-theme="light"] .app-window.expanded.preview-mode {
  width: 1192px !important;
  background: transparent !important;   /* 第 2 点问题根源：内容区也变太透 */
}
.app-window.preview-mode .directory-view {
  left: 472px !important; width: 248px !important; z-index: 2;
  background: var(--glass-strong);       /* 半透明，叠透明窗口 → 太透 */
}
.app-window.preview-mode .editor-panel {
  left: 0 !important; width: 472px !important; z-index: 1;
  background: var(--surface);            /* 半透明，叠透明窗口 → 太透 */
}
.app-window.preview-left .editor-panel  { transform: translateX(0) !important; }
.app-window.preview-right .editor-panel { transform: translateX(720px) !important; }
```

```js
// 交换：只改类，窗口不移动
async function applyPreviewSide(side) {
  previewSide = side;
  appEl.classList.toggle('preview-left', side === 'left');
  appEl.classList.toggle('preview-right', side === 'right');
}
```

## 六、请求建议

1. **透明区域残留**：具体是什么导致的？如何做到 1192 宽窗口里非内容区完全透明、无可见残留？
2. **内容太透明**：preview 时窗口背景 transparent 但目录/编辑内容区要保持**实体可读**，怎么处理？
   - 目录/编辑背景是否应为**不透明**（而非半透明玻璃）？
   - 还是窗口背景不该全透明，而只在透明区域（另一侧）透明？
3. 在确认上面两点前，是否值得继续做 `SetWindowRgn`？

## 附：当前系统状态

- 窗口在**浅色主题**下测试（`data-theme="light"`）
- 200% DPI，屏幕 3200×2000 物理
