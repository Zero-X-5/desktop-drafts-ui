# DESIGN_SYSTEM

## 产品定位

拾笺是一个轻量桌面草稿工具。界面应更像一个精致的桌面小物件，而不是传统的大型应用窗口。

## 固定尺寸

| 状态 | 宽度 | 高度 |
| --- | ---: | ---: |
| 收起条 | 248px | 36px |
| 目录页 | 248px | 480px |
| 目录 + 预览 | 720px | 480px |
| 预览内容宽度 | 472px | 480px |

这些尺寸既是 CSS 布局基线，也是桌面窗口尺寸基线。修改时必须同步检查渲染层和 `BrowserWindow`。

## 布局原则

- 目录页是固定锚点。
- 预览在右侧时，真实窗口左边缘与目录左边缘重合。
- 预览在左侧时，真实窗口左边缘向左偏移 472px，目录的屏幕坐标保持不变。
- 换边时只改变预览挂载侧，不播放目录与预览横穿交换的动画。
- 窗口跨越显示器中线后使用缓冲区判断方向，避免频繁抖动。

## 视觉语言

- 使用系统字体和现有 SVG 图标。
- 圆角、阴影、玻璃透明度和主题颜色以 `styles.css` 中的 CSS 变量为唯一来源。
- Windows 11 优先尝试 Acrylic；不可用时退回透明窗口与 CSS 半透明材质。
- 深色和浅色主题应保持相同的信息层级，不因主题变化改变尺寸和布局。

## 动效

- 展开、收起和预览开合使用短促的 `cubic-bezier(.22, 1, .36, 1)` 动画。
- 预览换边时目录页不得产生可见位移。
- 尊重 `prefers-reduced-motion`，减少动态效果时应接近即时切换。

## 桌面窗口规则

- 使用无边框、透明、不可手动缩放窗口。
- 仅标题拖动区域使用 `-webkit-app-region: drag`；按钮、输入框、菜单和编辑器必须是 `no-drag`。
- 渲染层不能直接访问 Node.js，只能使用 `preload.js` 暴露的最小窗口 API。
